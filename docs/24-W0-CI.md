# 24 — W0② CI 落地（三 job × 十道门；⑧ 由 W0-D 追加，见 §10；⑨ 由 W0-E 接线，见 §11；⑩ 由 LUM-1416 接线，见 §12）

> 切片：**LUM-1385 / W0-A**（`docs/plan1.md` §5 W0 ②、§6.2、§6.4）
> 分支：`feat/multica-rs-w0a-ci` → `feat/multica-rs-initial`
> 交付物：`.github/workflows/ci.yml`、`scripts/gates.sh`、本文件
> 结论：**七道门在本机全绿（含冷启动 + 全新数据库），exit 0；workflow 已做结构化校验，但尚未在 GitHub Actions 上实跑（见 §6）**

---

## 1. 为什么做这件事

`plan1.md` §7 的风险登记册里，**R2「无 CI → 质量债累积」的"概率"一栏写的是「已发生」**：
本仓历史上 M2-C 在 `cargo fmt` 不干净的树上合入主干，欠账 54 处，最后靠一个无关切片顺手清理。
根因不是"忘了跑 fmt"，而是**没有任何机器在合入前强制跑它** —— 门禁只存在于文档里。

顺带纠正一处文档漂移：本仓 `.github/` 在 `9b70556` 上**确实不存在**（不是"配了但没生效"）。
plan1 §6.2 画的 YAML 是**建议稿**，其中 `cargo nextest` / `cargo deny` / `schema_drift.py` /
`mc-conformance` 都**还没有落地前提**（本仓无 nextest 配置、无 deny.toml、无 schema 快照、
无 conformance crate）。本切片只固化**今天真实可跑**的七道门，未落地项见 §9
（⑦ 之后新增的 **⑧ `schema-drift`** 不是本切片交付的：它由 **W0-D / LUM-1402** 追加，配置见 §10）。

---

## 2. 三 job ↔ 十道门对应表

> **⑧ 是后加的**（W0-D / LUM-1402，见 §10），**⑨ 也是后加的**（W0-E / LUM-1413，见 §11），
> **⑩ 同样是后加的**（LUM-1416，见 §12）：本表已含它们，⑧ 之前的行是本切片（W0-A）的原始交付内容。

CI **不重写任何门禁命令**：每条 `run:` 都只是 `bash scripts/gates.sh --only <gate>`，
命令的唯一实现在 `scripts/gates.sh`。所以"本地怎么跑"和"CI 怎么跑"是逐字同一批命令，
不存在两处漂移（这是 gates.sh 存在的主要理由，不是省几个字符）。

| 门 | `--only <gate>` | 命令（`scripts/gates.sh` 内实现） | job | 需要 cargo | 需要 PG |
| --- | --- | --- | --- | :---: | :---: |
| ① | `fmt` | `cargo fmt --all --check` | `fast` | ✓ | — |
| ② | `build` | `cargo build --workspace --all-targets --locked` | `fast` | ✓ | — |
| ③ | `clippy` | `cargo clippy --workspace --all-targets -- -D warnings` | `fast` | ✓ | — |
| ④ | `clippy-test-util` | `cargo clippy -p mc-http --all-targets --features mc-http/test-util -- -D warnings` | `fast` | ✓ | — |
| ⑤ | `test` | `cargo test --workspace`（**剥掉 DB 变量**） | `fast` | ✓ | — |
| ⑥ | `db` | `cargo run -p mc-migrate -- run --dir migrations` → `cargo test -p mc-repos -p mc-http --features mc-http/test-util -- --ignored` | `db` | ✓ | ✓ |
| ⑦ | `route-parity` | `python3 scripts/route_parity.py --quiet` | `contract` | — | — |
| ⑧ | `schema-drift` | `python3 scripts/schema_drift.py --quiet`（判据=退出码；红了补打完整报告） | `db` | — | ✓ |
| ⑨ | `conformance` | `env -u MULTICA_TEST_DATABASE_URL -u MULTICA_DATABASE_URL cargo run -q -p mc-conformance -- --no-db --check crates/mc-conformance/report.json` | `contract` | ✓ | — |
| ⑩ | `file-size` | `python3 scripts/file_size_check.py --quiet`（R7 单文件 800 行硬上限；判据=退出码） | `fast` | — | — |

job 侧要点（`ci.yml`）：

| job | runner 前置 | 说明 |
| --- | --- | --- |
| `fast` | `actions/checkout@v4` → `dtolnay/rust-toolchain@stable`（显式 `components: rustfmt, clippy`）→ `Swatinem/rust-cache@v2` | ①–⑤ + ⑩ 各占一个 step，Actions UI 上直接看到红色的是哪道门 |
| `db` | 同上 + `services.postgres`（`postgres:16`，`--health-cmd "pg_isready -U multica -d multica"`，`--health-retries 20`） | job 级 `MULTICA_TEST_DATABASE_URL`；⑥ 在 gates.sh 内部**先迁移再跑 e2e**；⑧ 在 ⑥ 之后，前置一个"确保 `psql`/`python3`"步（⑧ 建/删自己的 scratch 库，不读目标库的表） |
| `contract` | `actions/checkout@v4` → `dtolnay/rust-toolchain@stable` → `Swatinem/rust-cache@v2` → `actions/setup-python@v5`（3.12）；`timeout-minutes: 30` | **离线、不连库**（也不 clone 上游）：⑦ 只读 `docs/fixtures/upstream-routes.tsv` 与 `docs/fixtures/route-parity-baseline.json`（纯 Python）；⑨ 要 Rust 工具链跑一次 `cargo run -p mc-conformance`，把 58 条 golden fixture 打在本仓真实 router 上回放、并与 `crates/mc-conformance/report.json`（stateless 层快照）逐字节比对，漂移即红 |

触发与并发：`push` + `pull_request`（`branches: [main, feat/multica-rs-initial]`）+ `workflow_dispatch`；
`concurrency.group = ci-<workflow>-<ref>` 且 `cancel-in-progress: true`（同分支 push 风暴不排队）。

---

## 3. 本地复现

```bash
# 一键：①–⑤ + ⑦ + ⑨ + ⑩（不需要数据库）
bash scripts/gates.sh

# 含真库 ⑥ 与 ⑧（库必须已存在；⑥ 的 schema 由本脚本自己建，⑧ 自己建/删 scratch 库）
bash scripts/gates.sh --with-db --db-url 'postgres://multica:multica@127.0.0.1:5432/multica_test'
MULTICA_TEST_DATABASE_URL='postgres://…' bash scripts/gates.sh --with-db

# 单门（CI 就是这么调的）
bash scripts/gates.sh --only fmt
bash scripts/gates.sh --only db
bash scripts/gates.sh --only schema-drift
bash scripts/gates.sh --only conformance
bash scripts/gates.sh --only file-size
bash scripts/gates.sh --list        # 列出 10 个闸门名
```

`scripts/gates.sh` 的行为契约：

* `set -u` + `set -o pipefail`；**不**用 `set -e` —— 默认是**收集式**（不 fail-fast），
  一道门红了继续跑完，最后给完整汇总表，便于一次看清"红了几道"。
* 每道门打印一行 `GATE_<NAME>_EXIT=<code>`（⑥ 另打 `GATE_DB_MIGRATE_EXIT` / `GATE_DB_E2E_EXIT`，
  `GATE_DB_EXIT` 是两者的合取）。末尾打印汇总表。
* 退出码：`0` = 选中的门全绿；`1` = 至少一道门非 0；`2` = 用法/前置条件错误
  （例如选了 ⑥ **或 ⑧** 却没给库 URL —— **绝不静默跳过**，这正是 `plan1` §6.4 第 4 条要的"不允许 skip"）。
* 脚本内部 `export PATH="$HOME/.cargo/bin:$PATH"`：本机 `/usr/bin/cargo` 是 1.75，解析不了本仓
  manifest（依赖里有 edition2024），不能靠调用者的 PATH。

---

## 4. 期望数字（CI 首次运行的对照基线）

下表的"冷启动实测"来自 **`cargo clean` 之后的完整 `--with-db` 跑**，并且**严格带上 `ci.yml` 的两个 env**
（`CARGO_TERM_COLOR=always`、`CARGO_INCREMENTAL=0`）—— 即 CI 首次运行的形态：空 `target/` + 全新数据库。
2026-09-22 本机实测：

| 门 | 命令 | 期望 | 冷启动实测 |
| --- | --- | --- | --- |
| ① | `cargo fmt --all --check` | exit 0 | **exit 0**（0s） |
| ② | `cargo build --workspace --all-targets --locked` | exit 0 | **exit 0**（50s） |
| ③ | `cargo clippy --workspace --all-targets -- -D warnings` | exit 0 | **exit 0**（28s） |
| ④ | `cargo clippy -p mc-http --all-targets --features mc-http/test-util -- -D warnings` | exit 0 | **exit 0**（8s） |
| ⑤ | `cargo test --workspace`（不带 DB 变量） | 233 passed / 0 failed / 43 ignored（45 个 test target） | **完全一致**（50s） |
| ⑥ | `mc-migrate run --dir migrations` + `cargo test -p mc-repos -p mc-http --features mc-http/test-util -- --ignored` | **77 passed** / 0 failed | **77 passed**（39s，含迁移） |
| ⑦ | `python3 scripts/route_parity.py --quiet` | exit 0；implemented **124/456**、known_gap 332、unclaimed 0、regression 0、local_only 12 | **完全一致**（0s） |
| ⑧ | `python3 scripts/schema_drift.py --quiet`（**W0-D 后加**，真 PG16） | exit 0；`missing 460 / extra 140 / differs 158 / apply-exception 9 = 767` 全部已登记、登记表 25 行 0 stale | **exit 0**（~1.3s，见 `docs/30` §4） |
| ⑨ | `cargo run -q -p mc-conformance -- --no-db --check crates/mc-conformance/report.json`（**W0-E 后加**，stateless 层） | exit 0；`pass 4 / mismatch 1 / unmounted 5 / placeholder 1 / unevaluable 47`，契约等价率 **4/58 = 6.9%**，与 `report.json` 逐字节一致 | **exit 0**（~1s；首跑含一次 `cargo run`，暖缓存 0–11s） |
| ⑩ | `python3 scripts/file_size_check.py --quiet`（**LUM-1416 后加**，纯 Python + `git ls-files`） | exit 0；`scanned 123 / baseline 14 / violations 0`（基线 14 条都是存量违规，逐条只允许变短） | **exit 0**（~0.2s，见 §12） |

汇总表（CI 环境变量 + 冷启动 + 全新库，exit **0**）—— 这是 **W0-A 时期（7 门，默认集合 = ①–⑤ + ⑦）** 的原始记录，
保留不改；⑧ / ⑨ / ⑩ 接线后的实测汇总表在 §10 / §11 / §12：

```text
============================= GATE SUMMARY =============================
  #  gate               exit   time  result
  ①  fmt                   0     0s  PASS
  ②  build                 0    50s  PASS
  ③  clippy                0    28s  PASS
  ④  clippy-test-util      0     8s  PASS
  ⑤  test                  0    50s  PASS
  ⑥  db                    0    39s  PASS  (migrate=0,e2e=0)
  ⑦  route-parity          0     0s  PASS
-----------------------------------------------------------------------
  overall: PASS — 7/7 gate(s) green in 175s
=======================================================================
```

⑥ 的 77 条按 target 拆开（与 `plan1.md` §6.4 及本切片立项时的实测逐条对齐）：

| target | passed | target | passed |
| --- | ---: | --- | ---: |
| `mc-repos` unittests（`src/**/db_tests`） | 43 | `tests/issues.rs` | 6 |
| `tests/contract_gaps.rs` | 8 | `tests/pats.rs` | 4 |
| `tests/comments.rs` | 6 | `tests/inbox.rs` | 4 |
| `tests/invitations.rs` | 3 | `tests/share_links.rs` | 3 |
| **合计** | | | **77** |

⑦ 的完整输出：

```text
upstream 456 (commit f41fae6b08fb) | local 139 registered | baseline 139
  implemented  111 real +  13 placeholder =  124 / 456   known_gap  332   unclaimed    0   regression   0   local_only   12
OK: every upstream route is either implemented or owned
```

> 首次运行 CI 时，若这些数字**变大**是正常的（后续切片在加路由/加测试）；**变小**或 `regression`
> 非 0 才是需要解释的事。②③④⑤ 的耗时在 CI（`ubuntu-latest` + rust-cache 冷/暖）会明显长于本机。

---

## 5. 本机自检记录（交付证据）

环境：本机 `/usr/bin/cargo` 1.75 不可用，实际工具链 `~/.cargo/bin` 的 **rustup stable 1.98.1**
（rustfmt 1.9.0 / clippy 0.1.98）；PostgreSQL 16 @ 127.0.0.1:5432。

1. **YAML 可解析**（本机装了 PyYAML 6.0.1）：
   `python3 -c "import yaml;yaml.safe_load(open('.github/workflows/ci.yml'))"` → 无异常。
   再用一个 38 项结构化核对脚本断言：3 个 job（`fast`/`db`/`contract`，顺序）、7 个 `run:` step 全部
   是 `bash scripts/gates.sh --only <已知门>`、7 道门无遗漏、`fast` 的三件前置 action 与
   `components: rustfmt, clippy`、`fast` 的 env/命令里**没有** DB 变量、`db` 的镜像/健康检查/端口、
   `contract` 只用 python → **ALL CHECKS PASSED**。
2. **workflow 里每条 `run:` 在本机逐条跑过**，7/7 exit 0（含用**全新空库**跑 ⑥，
   等价于 CI 的 service 容器）：

   ```text
   job=fast     step=① bash scripts/gates.sh --only fmt              => exit 0   GATE_FMT_EXIT=0
   job=fast     step=② bash scripts/gates.sh --only build            => exit 0   GATE_BUILD_EXIT=0
   job=fast     step=③ bash scripts/gates.sh --only clippy           => exit 0   GATE_CLIPPY_EXIT=0
   job=fast     step=④ bash scripts/gates.sh --only clippy-test-util => exit 0   GATE_CLIPPY_TEST_UTIL_EXIT=0
   job=fast     step=⑤ bash scripts/gates.sh --only test             => exit 0   GATE_TEST_EXIT=0
   job=db       step=⑥ bash scripts/gates.sh --only db               => exit 0   GATE_DB_MIGRATE_EXIT=0 / GATE_DB_E2E_EXIT=0 / GATE_DB_EXIT=0
   job=contract step=⑦ bash scripts/gates.sh --only route-parity     => exit 0   GATE_ROUTE_PARITY_EXIT=0
   ```
3. **`bash -n scripts/gates.sh`** 通过；`Cargo.toml` / `Cargo.lock` / `crates/**` / `apps/**` **零改动**
   （本切片只新增 `.github/workflows/ci.yml`、`scripts/gates.sh`、`docs/24-W0-CI.md`，并在
   `docs/plan1.md` §6.2 加一段落地标注）。

> 自检小坑（给别人复核时省 10 分钟）：`python3 -c "import yaml; yaml.safe_load(...)"` 只做解析不做断言。
> 而且 **PyYAML 遵循 YAML 1.1，会把裸键 `on:` 解析成布尔 `True`** —— 这不是 workflow 写错了
> （GitHub Actions 的解析器按字符串 `on` 处理，全世界的 workflow 都这么写），只是别拿 PyYAML 的
> key 列表去判 `"on" in doc`。

---

## 6. 「Actions 是否实跑过」——诚实说明

**本切片交付的是 workflow 文件 + 一次已验证的本地七道门全绿，不是一次绿色的 Actions 跑。**
但"Actions 能不能认这个文件"已经实测过了：

* **Actions 在本仓是启用的，且已接受并调度了本 workflow**（实测，本切片 push 后）：
  `GET /repos/louloulin/paperclip-rs/actions/workflows` → `name: CI`、`path: .github/workflows/ci.yml`、
  `state: active`；`GET /actions/runs?head_sha=<本切片 head>` → `total_count: 1`，
  run `35736457657`，`event: pull_request`。
  这证明 YAML 通过了解析与 schema 校验（语法错的文件不会注册成 active、也不会被调度），
  也证明**不需要人工在仓库设置里做任何事**。
* **但该 run 的结论本切片不等**：切片明确要求不 `gh run watch` / 不 sleep 轮询，
  验收依据是 §5 的本地 exit code + §4 的期望数字，不是 Actions 的绿勾。见到的状态是 `queued`。
* 若首次 Actions 跑红了：**先看是不是工具链/资源差异**（见下一段），再看是不是 §7 的坑被"顺手优化"掉。
  本机在**严格带 `ci.yml` 同款 env** 下的冷启动是 7/7 绿（§4），所以红的位置本身就是信息。
* **已知差异**：CI 跑在 `ubuntu-latest` + 最新 stable，本机是 stable 1.98.1。
  两者若在不同小版本上对 fmt/clippy 有差异（真发生过：fmt 风格随版本微调），**以 CI 为准**。
  在 CI 变成强制门禁之前，本机 `bash scripts/gates.sh` 是本地唯一的真相来源。
* **本切片不配 branch protection、不做 "CI 必须绿才能 merge" 的强制**（那是仓库设置，超出切片范围，
  见 §9）。所以 CI 现在是「**信号**」，还不是「**闸门**」。

---

## 7. 例外与坑（都是本仓实测出来的，别"顺手优化"掉）

1. **⑤ 绝不能带 `MULTICA_TEST_DATABASE_URL`**。`crates/mc-http/Cargo.toml` 把根 `tests/smoke.rs`
   声明成名为 `smoke` 的集成 test target（虚拟 workspace 根没有 `[package]`，不声明就没人编译它）；
   该 target 里的 `workspace_member_http_e2e` 一拿到库就对**同一个库重跑迁移** →
   `relation "user" already exists`。所以 gates.sh 在 ⑤ 上用 **`env -u MULTICA_TEST_DATABASE_URL -u MULTICA_DATABASE_URL`**
   显式剥掉这两个变量 —— 即使调用者（或 CI job 的 `env:`）已经 export 过它，⑤ 也是安全的。
   *实测*：`MULTICA_TEST_DATABASE_URL=<真库> bash scripts/gates.sh --only test` → exit 0。
2. **③ 与 ④ 不可合并**。只有 `-p mc-http --features mc-http/test-util` 这一遍会检查
   `crates/mc-http/tests/*`（comments / contract_gaps / inbox / invitations / issues / pats / share_links）
   的 DB e2e 代码；workspace 那一遍**不覆盖**它们（feature 未开）。少了 ④，这 7 个文件的 warning 会漏网。
3. **⑥ 必须先建表**。`crates/mc-repos/src/**/db_tests` 与 `crates/mc-http/tests/*` 直接
   `INSERT INTO workspace`，**自己不做迁移** ⇒ `db` job 必须先 `mc-migrate run --dir migrations`。
   gates.sh 把这个前置放进了 `db` 门内部（而不是让 YAML 里再写一条命令），这样 `--only db`
   永远是"自足的"，挪到别处也不会漏。
4. **⑥ 的 DB 测试不清行**（`user` / `workspace` / `issue` 会残留）。带残留重跑仍绿，所以本机反复跑
   `--only db` 是安全的；CI 每次都是全新 service 容器，不涉及清理。（别在别人正跑 ⑥ 的时候去删行。）
5. **`--only` 与 `--with-db` 互斥**。`--with-db` 是"默认集合 + ⑥"；要精确指定就用 `--only`，
   两个一起给会 exit 2 —— 语义重叠时宁可报错，不要猜。
6. **⑨ 必须显式 `--no-db`（并剥掉库变量）**。`crates/mc-conformance/src/main.rs` 的 `--db-url` 声明了
   `env = "MULTICA_TEST_DATABASE_URL"`：谁 export 过这个变量（跑 ⑥/⑧ 的人都会），回放器就会追加 database 层，
   再把「两层取强者」的**合并报告**拿去和 `report.json`（stateless 层快照）比 → **门会因环境而红**。
   与 ⑤ 同一个套路：gates.sh 用 `env -u MULTICA_TEST_DATABASE_URL -u MULTICA_DATABASE_URL` 剥变量，
   同时显式给 `--no-db`（两道保险，任一生效都不会追库）。实测见 §11.2 的 RUN 3。

---

## 8. 未做 / 明确不做的（避免误以为"CI 已完备"）

| 项 | 状态 | 原因 |
| --- | --- | --- |
| `.github/ci-paths.json` 路径过滤 | **不做** | 收益（省几分钟）远小于风险（改 `crates/**` 却跳过 build）。`plan1` §6.4 要的是"必须全绿"，不是"尽量少跑"。宁缺毋滥。 |
| branch protection / 强制合并门禁 | **不做** | 仓库设置，超出本切片；见 §6 |
| `README` CI 徽章 | **不做** | 可选；徽章要有**完成过**的 run 才有意义（现在只有刚调度的 queued run），否则显示 "no status" 反而误导。且徽章是装饰不是门禁。 |
| `cargo nextest` / `cargo deny` | **未落地** | 本仓无 nextest 配置、无 `deny.toml` —— 都是独立切片的前置，塞进 `ci.yml` 只会让 CI 红着躺尸（`plan1` §6.3 的 drift 对账属 **W0③**） |
| `[workspace.lints]` 在成员 crate 推广（`lints.workspace = true`） | **不做** | 那是 **W0①** 的活（要动 17 个成员 `Cargo.toml`，与所有并发切片冲突） |
| 缓存/并行优化、矩阵构建 | 不做 | 冷启动 225s，还没到需要优化的量级 |

---

## 9. 放行后续切片的前提

* 本切片只碰 **`.github/workflows/ci.yml`（新增）、`scripts/gates.sh`（新增）、`docs/24`（新增）**，
  以及 `docs/plan1.md` §6.2 的一处"已落地"标注；**不动 `crates/**`、不动 `Cargo.toml`/`Cargo.lock`**，
  因此与并发切片（M2-D / M2-E / M3）无冲突面。
* 后续切片交付时，把 §4 的期望数字当**下限**核对：路由数、测试数只会增；`regression` 必须始终为 0
  （有意的路由退役要 `--write-baseline`，流程见 `docs/22` §4.4）。
* 想加新门（例如 W0③ 的 schema drift）：在 `scripts/gates.sh` 里加一个 gate + 在 `ci.yml` 里加一个
  `--only <gate>` step，**不要把命令直接写进 YAML**。
  > 已按此办理：**⑧ `schema-drift` 已由 W0-D 接线**（LUM-1402，配置与实测见 §10 与 `docs/30`）；
  > **⑨ `conformance` 已由 W0-E 接线**（LUM-1413，见 §11 与 `docs/27` §9）；
  > **⑩ `file-size` 已由 LUM-1416 接线**（R7 的单文件 800 行上限，见 §12）。

---

## 10. 追加：门 ⑧ `schema-drift` 的接线（W0-D / LUM-1402，2026-09-22）

§9 最后那条"想加新门就这么加"已经执行。此处只记**对本文件其余部分的增量**（完整交付说明与实测输出
见 `docs/30-W0-DRIFT-GATE.md`）：

| 项 | 值 |
| --- | --- |
| 门名 / 编号 / 打分变量 | `schema-drift` / ⑧ / `GATE_SCHEMA_DRIFT_EXIT` |
| 命令 | `MULTICA_TEST_DATABASE_URL=<db-url> python3 scripts/schema_drift.py --quiet` |
| 挂在 | `db` job（**不能挂 `contract`**：那道门纯离线，⑧ 要真 PG） |
| `--with-db` | ①–⑤ + ⑥ + ⑧ + ⑦（8 门） |
| 缺库 URL | 与 ⑥ 同一条前置检查 → `exit 2`（不静默跳过） |
| 红门诊断 | 非 0 时**再跑一遍不带 `--quiet`** 的，打印未登记差异逐行（绿时那份报告有 767 行） |
| deps step | `⑧ deps — psql client + python3 for schema-drift`：缺 `psql` 才 `apt-get install postgresql-client` |
| 与 ⑥ 的分工 | ⑥ = 迁移跑得动 + e2e 过；⑧ = 跑出来的 schema 还是不是上游那份 |

⑧ 的判定边界（**限制**，不是缺陷）：`contracts/schema-deviations.tsv` 里有 **11 行 `*` glob**
（`missing` 的 table/column/constraint/index/function/trigger、`extra` 的 column/constraint/index、
`differs` 的 column/constraint），这些 category 下**将来新出现的**差异会被自动登记、⑧ 不会响；
新增 `extra table` 之类仍会红（实测：塞一条 `CREATE TABLE gate_probe_bogus` → `GATE_SCHEMA_DRIFT_EXIT=1`）。
收窄那 11 行是 **LUM-1387（W0-B2）** 的验收项。

---

## 11. 追加：门 ⑨ `conformance` 的接线（W0-E / LUM-1413，2026-09-22）

`docs/27-W0-GOLDEN-FIXTURES.md` §9 第 1 条预留的「⑨ 契约门」已接线（W0-C 当时的文件范围不含
`scripts/gates.sh` / `ci.yml`）。本切片**不新增 Rust 代码**，只接线 + 文档；`contracts/**`、`crates/**`、
`migrations/**`、`Cargo.lock` **零改动**。

| 项 | 值 |
| --- | --- |
| 门名 / 编号 / 打分变量 | `conformance` / ⑨ / `GATE_CONFORMANCE_EXIT` |
| 命令 | `env -u MULTICA_TEST_DATABASE_URL -u MULTICA_DATABASE_URL cargo run -q -p mc-conformance -- --no-db --check crates/mc-conformance/report.json` |
| 判据 | 退出码（`0` 一致 / `1` 漂移 / `2` 用法或加载错 / `3` `--require-pass` 未达标）；漂移时回放器自己打印**第一处差异**（行号 + committed / fresh 两行） |
| 分类 | **离线确定性门**（stateless 层 `connect_lazy` 指向不可达端口，不拨号、不建库）⇒ 进**默认集合**，与 ⑦ 同组 |
| 挂在 | `contract` job（该 job 因此加 `dtolnay/rust-toolchain@stable` + `Swatinem/rust-cache@v2`，`timeout-minutes` 15 → 30；它仍然**不连库、不 clone 上游**） |
| 与 ⑦ 的分工 | ⑦ 问「上游 456 条路由都有归属吗」；⑨ 问「已抽出的 58 条 golden fixture 打在本仓真 router 上，结论还是不是快照里那样」 |

### 11.1 为什么 ⑨ 要 `--no-db` + `env -u`（不是多余的保险）

`report.json` 固定是 **stateless 层**快照（`docs/27` §7 第 3 条），而 `--db-url` 绑定了
`MULTICA_TEST_DATABASE_URL`。若 ⑨ 在这两个变量存在时运行，回放器会追 database 层、把**合并报告**
拿去比 stateless 快照，`--check` 必然报漂移 —— 那是**环境差异冒充契约漂移**，比红灯本身更坏。
`§11.2` 的 RUN 3 就是这个探针。

### 11.2 实测（2026-09-22，本机 stable 1.98.1，暖 `target/`）

```console
# RUN 1 / RUN 2：同一命令连跑两次（确定性；两次都绿且结论逐字相同）
$ bash scripts/gates.sh --only conformance
  pass 4  mismatch 1  unmounted 5  placeholder 1  unevaluable 47
  契约等价率 = 4/58 = 6.9%
  report matches crates/mc-conformance/report.json
GATE_CONFORMANCE_EXIT=0
  overall: PASS — 1/1 gate(s) green in 1s        # 第二次 0s

# RUN 3：故意泄漏库变量（必须不追 database 层，结论与 RUN 1 相同）
$ MULTICA_TEST_DATABASE_URL='postgres://nobody:nope@127.0.0.1:1/nope' bash scripts/gates.sh --only conformance
  report matches crates/mc-conformance/report.json
GATE_CONFORMANCE_EXIT=0

# RUN 4：负向探针 —— 临时把 contracts/golden/me/001-TestProtectedRoutesRequireAuth-L433.json
#        的期望码 401 → 403（不提交）
$ bash scripts/gates.sh --only conformance
  pass 3  mismatch 2  unmounted 5  placeholder 1  unevaluable 47
conformance report drifted from crates/mc-conformance/report.json: 重新运行 mc-conformance --write crates/mc-conformance/report.json 并解释差异
  first difference at line 10:
    committed:     "pass": 4,
    fresh:         "pass": 3,
GATE_CONFORMANCE_EXIT=1
  overall: FAIL — 0/1 gate(s) green in 1s (rerun the red gate(s) with --only)
$ git status --porcelain   # RUN 5：把 fixture 复原（只剩本切片改的 5 个文件，fixture 无改动）→ 再次 exit 0

# RUN 6 / RUN 7：默认集合连跑两次（第二次 9s，结论相同）
$ bash scripts/gates.sh
  ① ② ③ ④ ⑤ ⑦ ⑨ → 7/7 PASS
  overall: PASS — 7/7 gate(s) green in 47s      # 第二次 9s；⑦ 与 ⑨ 都是 0s

# RUN 8：九门（含真 PG16）
$ bash scripts/gates.sh --with-db --db-url 'postgres://mc_lum1413:***@127.0.0.1:5432/multica_lum1413'
  ①  fmt 0s     ②  build 0s    ③  clippy 1s   ④  clippy-test-util 0s  ⑤  test 8s
  ⑥  db 17s PASS (migrate=0,e2e=0)     ⑧  schema-drift 2s PASS
  ⑦  route-parity 1s PASS              ⑨  conformance 0s PASS
  overall: PASS — 9/9 gate(s) green in 29s
$ bash scripts/gates.sh --list                 # 9 行（末行 = conformance）
```

`--list` 现在是 9 行、末行为 `conformance`；默认集合 = **①–⑤ + ⑦ + ⑨**，`--with-db` = **①–⑨**。

### 11.3 CI 侧（`.github/workflows/ci.yml`）

`contract` job 更名 `contract — route parity + conformance`，step 顺序：

```text
checkout → rust-toolchain(stable) → rust-cache → setup-python(3.12) → ⑦ route-parity → ⑨ conformance
```

两条 `run:` 仍然只是 `bash scripts/gates.sh --only <gate>`（命令的唯一实现仍在 `gates.sh`）。
Actions 上的实跑口径同 §6：**本切片不等**（验收依据是本地 exit code 与接线正确性）。

### 11.4 已登记的非等价项会不会污染默认集合

不会。⑨ 比的是「同一层次的快照」，不是「零 mismatch」：`pass 4 / mismatch 1 / unmounted 5 /
placeholder 1 / unevaluable 47` 都是 `report.json` **里已经写着的**数字，门只问「现在重跑还是不是这样」。
所以 `LUM-1410`（3 条 `POST /api/issues` 应 400 实测 201，属 database 层）与 `/auth/google` 那条
**回放器限制**（stateless 层固定 mismatch）都不需要放宽口径：修好它们反而要让 `report.json` 跟着刷新
（`docs/27` §9 第 1 条的强制对账）。

---

## 12. 追加：门 ⑩ `file-size` 的接线（LUM-1416 / R7 收口，2026-09-22）

`docs/plan1.md` §7 风险登记 **R7** 的处方里写着「`scripts/file_size_check.py`（800 行上限）」——
该脚本**从未存在**，`gates.sh` 也没有尺寸门，所以这条控制事实上无人执行（`docs/plan1.md` 只说"用这个脚本"，
从未说它已在跑；`docs/14-M2-TABLE.md` §6.4 当时就把它记成"已登记、未修"）。
本切片把脚本 + 门 + 基线补上，并把 M2-D 新引入的 1789 行文件拆回 800 行以内。

| 项 | 值 |
| --- | --- |
| 门名 / 编号 / 打分变量 | `file-size` / ⑩ / `GATE_FILE_SIZE_EXIT` |
| 命令 | `python3 scripts/file_size_check.py --quiet`（判据=退出码；红了打印违规表） |
| 挂在 | `fast` job（**不新增 job、不新增 action 版本**；⑩ 只读工作区，不需要 cargo / PG / psql） |
| 分类 | **离线确定性门**（纯 Python stdlib + `git ls-files`，亚秒级）⇒ 进**默认集合**，排在 ⑨ 之后 |
| 默认集合 | ①–⑤ + ⑦ + ⑨ + ⑩ = **8 门**；`--list` = **10 行**（编号不重编、追加在末尾） |
| `--with-db` | ①–⑩ = **10 门** |
| 存量违规 | `scripts/file_size_baseline.tsv`（14 条，`path<TAB>lines`）：**基线只减不增，新增违规不得写进白名单**；刷新用 `--write-baseline`（输出贴 PR） |
| 判据 | 清单外文件 > 800 → 红；清单内 > 记录行数 → 红；清单内 ≤ 800 → 红（已达标请删）；清单内文件已不在 git → 红（请删） |

检查范围只有**代码**：`crates/**/*.rs`、`apps/**/*.rs`、`scripts/*.py`、`scripts/*.sh`、
`.github/workflows/*.yml`（按前缀+后缀匹配，深度不限）；`docs/**/*.md` **不查**（长文档是有意为之）。
行数 = 换行符个数（末行无换行但有内容时 +1，与 `wc -l` 对良构文件一致）。

### 12.1 实测（2026-09-22，本机 stable 1.98.1，暖 `target/`）

```console
# RUN 1：清单
$ bash scripts/gates.sh --list | cat -n | tail -2
     9	conformance
    10	file-size

# RUN 2：默认集合（不需要数据库）
$ bash scripts/gates.sh
  ① fmt 3s  ② build 37s  ③ clippy 24s  ④ clippy-test-util 15s  ⑤ test 13s
  ⑦ route-parity 0s   ⑨ conformance 22s   ⑩ file-size 0s
  overall: PASS — 8/8 gate(s) green in 114s

# RUN 3：十门（含真 PG16：⑥ 迁移 + DB e2e、⑧ schema-drift 不回归）
$ MULTICA_TEST_DATABASE_URL='postgres://…' bash scripts/gates.sh --with-db
  ① fmt 1s  ② build 0s  ③ clippy 0s  ④ clippy-test-util 1s  ⑤ test 23s
  ⑥ db 32s PASS (migrate=0,e2e=0)   ⑧ schema-drift 6s PASS
  ⑦ route-parity 0s PASS  ⑨ conformance 0s PASS  ⑩ file-size 0s PASS
  overall: PASS — 10/10 gate(s) green in 63s

# RUN 4：负向探针 —— 临时把 crates/mc-core/src/lib.rs（不在基线里）补到 801 行（不提交）
$ bash scripts/gates.sh --only file-size
VIOLATIONS (1):
   lines  limit baseline  path                       reason
     801    800        -  crates/mc-core/src/lib.rs  不在基线里且超过 800 行上限
GATE_FILE_SIZE_EXIT=1
  overall: FAIL — 0/1 gate(s) green in 0s (rerun the red gate(s) with --only)

# RUN 5：把该文件复原 → ⑩ 再次 exit 0（`git status --porcelain` 无该项）

# RUN 6：另外三条基线规则（用真基线的副本 `/tmp/probe.tsv` 与前缀造，不碰真基线）
$ cp scripts/file_size_baseline.tsv /tmp/probe.tsv
$ sed -i 's/^scripts\/schema_drift.py\t807$/scripts\/schema_drift.py\t806/' /tmp/probe.tsv   # 记录值低于实际
$ sed -i 's/^crates\/mc-repos\/src\/invitation.rs\t828$/…\t700/'            /tmp/probe.tsv   # 记录了一个已达标文件
$ printf 'crates/mc-gone/src/lib.rs\t900\n' >> /tmp/probe.tsv                                   # 记录了一个已消失文件
$ python3 scripts/file_size_check.py --baseline /tmp/probe.tsv
VIOLATIONS (3):
     900    800      900  crates/mc-gone/src/lib.rs          已不在 git 中，请从基线删除
     828    800      700  crates/mc-repos/src/invitation.rs  超过基线记录（700 行），只允许变短
     807    800      806  scripts/schema_drift.py            超过基线记录（806 行），只允许变短

# 「已达标请删」那一支需要 `lines <= limit` 才有意义，用更宽的上限在**真基线**上探：
$ python3 scripts/file_size_check.py --limit 900 --quiet
     831    900      831  crates/mc-http/src/routes/comments.rs  已达标（<= 900 行），请从基线删除
     828    900      828  crates/mc-repos/src/invitation.rs      已达标（<= 900 行），请从基线删除
     807    900      807  scripts/schema_drift.py                已达标（<= 900 行），请从基线删除
```

注意 `--limit` 与基线是**绑定**的：基线是用默认 800 生成的，用别的上限跑就会触发「已达标请删」——
这是有意为之（基线必须用默认上限生成，否则门会在默认上限下红）。

⑩ 与 `docs/14-M2-TABLE.md` §6.4 是同一次收口：M2-D 的 1789 行
`crates/mc-http/src/routes/issue_table.rs` 已拆成 `mod.rs` 509 / `spec.rs` 790 / `cursor.rs` 145 /
`tests.rs` 433（纯移动、零行为变化），并**从基线里消失**；基线剩下的 14 条都是本切片之前就存在的
存量违规（`routes/issues.rs` 2227、`mc-repos/src/issue.rs` 1950、`routes/auth.rs` 1704、…），只允许变短。
