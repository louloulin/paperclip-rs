[package]
name = "pc-plugin-stream-bus"
version = "0.1.0"
edition = "2021"
description = "In-memory pub/sub for plugin SSE streams"
license = "Apache-2.0"

[dependencies]
serde_json = "1"

[lib]
path = "src/lib.rs"
