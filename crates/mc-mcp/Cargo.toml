[package]
name = "mc-mcp"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "M6 anchor scaffold 占位 — remote MCP 客户端（JSON-RPC over HTTP + OAuth + dev-origin 白名单）"

[lints]
workspace = true

[features]
default = []

# M6-0 anchor（LUM-1665 / docs/57-M6-PLAN.md §5）：依赖一次声明到位，此后 M6 各切片
# **不得**再新增三方依赖（要加走 docs/15 §8.4 仲裁，由集成方加）。
[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
url = { workspace = true }
base64 = { workspace = true }
reqwest = { workspace = true }
tokio = { workspace = true }

mc-errors = { path = "../mc-errors" }
