[package]
name = "pc-pipeline-health"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Pipeline setup-health warnings (port of paperclip packages/shared/src/pipeline-health.ts)"

[lints]
workspace = true

[dependencies]
regex = { version = "1", default-features = false, features = ["std", "unicode-case", "unicode-perl", "unicode-script"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
