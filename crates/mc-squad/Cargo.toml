[package]
name = "mc-squad"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "M4 anchor scaffold 占位 — squad / squad_member 领域层，由 M4-2 按需填充"

[lints]
workspace = true

[features]
default = []

# M4 anchor scaffold（LUM-1470 / docs/42-M4-PLAN.md §5.1）：依赖一次声明到位，
# 此后 M4 各切片**不得**再新增三方依赖（要加走 docs/15 §8.4 仲裁，由集成方加）。
[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
uuid = { workspace = true }
chrono = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }

mc-core = { path = "../mc-core" }
mc-db = { path = "../mc-db" }
mc-errors = { path = "../mc-errors" }
