[package]
name = "pc-connection-display"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Humanize connection / tool identifiers for prosumer UI surfaces (port of paperclip packages/shared/src/humanize-connection.ts)"

[lints]
workspace = true

[dependencies]
once_cell = "1"
regex = { version = "1", default-features = false, features = ["std", "unicode-case", "unicode-perl", "unicode-script"] }
