[package]
name = "pc-adapter-models-env"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Per-adapter model list from PAPERCLIP_ADAPTER_MODELS env — 1:1 with paperclip server adapter-models-env.ts"

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
