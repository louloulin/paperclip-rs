[package]
name = "pc-invite-rate-limit"
version = "0.1.0"
edition = "2021"
description = "Generic per-IP sliding-window rate limiter for invite-token endpoints"
license = "Apache-2.0"

[dependencies]

[lib]
path = "src/lib.rs"
