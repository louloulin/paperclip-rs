[package]
name = "pc-remote-http-endpoint-guard"
version = "0.1.0"
edition = "2021"
description = "Remote HTTP endpoint URL validation + private/reserved IP guard (DNS injectable)"
license = "Apache-2.0"

[dependencies]
async-trait = { workspace = true }
thiserror = "1"
tokio = { workspace = true }
url = "2"

[lib]
path = "src/lib.rs"
