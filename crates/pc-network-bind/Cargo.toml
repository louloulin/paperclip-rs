[package]
name = "pc-network-bind"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Network bind mode validation and runtime resolution (port of paperclip packages/shared/src/network-bind.ts)"

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
