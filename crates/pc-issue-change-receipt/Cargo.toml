[package]
name = "pc-issue-change-receipt"
version = "0.1.0"
edition = "2021"
description = "Build IssueChanges receipt from before/after maps"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
path = "src/lib.rs"
