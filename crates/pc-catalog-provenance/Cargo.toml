[package]
name = "pc-catalog-provenance"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Portable catalog provenance — extracts catalog source/origin metadata from arbitrary nested values"

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
