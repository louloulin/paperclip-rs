[package]
name = "pc-sidebar"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Sidebar helpers (badge counters + order preference normalization)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }

[lib]
path = "src/lib.rs"
