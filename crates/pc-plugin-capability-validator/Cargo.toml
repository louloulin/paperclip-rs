[package]
name = "pc-plugin-capability-validator"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Plugin capability validator: install-time manifest checks + runtime operation gate (1:1 with Node services/plugin-capability-validator.ts)"

[lints]
workspace = true

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
thiserror = { workspace = true }
tracing = "0.1"

[dev-dependencies]
serde_json = { workspace = true }
