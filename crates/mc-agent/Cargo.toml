[package]
name = "mc-agent"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "M3 anchor scaffold 占位 — agent 领域层，由 M3-5 按需填充（无需领域逻辑时保持空 crate）"

[lints]
workspace = true

[features]
default = []

# M3 anchor scaffold（LUM-1406 / docs/15-M3-PLAN.md §7.2.5）：依赖一次声明到位。
# docs/15 §5 M3-5 写明本 crate「只在需要领域逻辑时才创建（否则本片仅 repo + 路由）」——
# 本片按 §7.2.1 的「建议一次建齐」建出来，成本是 0；M3-5 若判定不需要，可保持空实现。
[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
uuid = { workspace = true }
chrono = { workspace = true }
tokio = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }

mc-core = { path = "../mc-core" }
