[package]
name = "pc-plugin-manifest-validator"
version = "0.1.0"
edition = "2021"
description = "Plugin manifest parser/validator (parse / parseOrThrow / supported versions)"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
path = "src/lib.rs"
