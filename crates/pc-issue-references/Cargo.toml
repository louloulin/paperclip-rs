[package]
name = "pc-issue-references"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Issue reference parsing for cross-referencing issues from text/markdown (port of paperclip packages/shared/src/issue-references.ts — pure parts)"

[lints]
workspace = true

[dependencies]
once_cell = "1"
regex = { version = "1", default-features = false, features = ["std", "unicode-case", "unicode-perl", "unicode-script"] }
url = "2"
