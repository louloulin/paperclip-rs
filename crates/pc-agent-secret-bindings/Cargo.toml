[package]
name = "pc-agent-secret-bindings"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Agent secret binding sync — extracts secret_ref / user_secret_ref from adapter config + dispatches to secrets service"

[lints]
workspace = true

[dependencies]
async-trait = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tracing = "0.1"

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt", "rt-multi-thread"] }
