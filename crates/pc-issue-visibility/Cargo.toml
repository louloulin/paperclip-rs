[package]
name = "pc-issue-visibility"
version = "0.1.0"
edition = "2021"
description = "Visible-issue SQL condition + raw SQL fragment"
license = "Apache-2.0"

[dependencies]

[lib]
path = "src/lib.rs"
