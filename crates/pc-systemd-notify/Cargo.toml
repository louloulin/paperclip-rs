[package]
name = "pc-systemd-notify"
version = "0.1.0"
edition = "2021"
description = "systemd-notify(1) wrapper with injectable executor"
license = "Apache-2.0"

[dependencies]
tokio = { version = "1", features = ["sync", "rt", "macros"] }

[lib]
path = "src/lib.rs"
