[package]
name = "pc-inbox-agent-policy"
version = "0.1.0"
edition = "2021"
description = "Inbox agent policy pure helpers"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }

[lib]
path = "src/lib.rs"
