[package]
name = "mc-runtime"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "M3 anchor scaffold 占位 — runtime 抽象/目录/存活探测/配额，由 M3-2 填充"

[lints]
workspace = true

[features]
default = []

# M3 anchor scaffold（LUM-1406 / docs/15-M3-PLAN.md §7.2.5）：依赖一次声明到位。
# `async-trait` + `tokio` 给 `RuntimeAdapter` trait 与 spawn/流式读取（M3-2），
# `mc-core` 给 `Id`；三个版本都已在 `Cargo.lock` 里，无新增三方包。
[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
uuid = { workspace = true }
chrono = { workspace = true }
tokio = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
async-trait = { workspace = true }

mc-core = { path = "../mc-core" }
