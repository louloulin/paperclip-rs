[package]
name = "pc-agent-permissions"
version = "0.1.0"
edition = "2021"
description = "Agent permissions normalization"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
path = "src/lib.rs"
