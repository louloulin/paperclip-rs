[package]
name = "pc-decision-wakeup"
version = "0.1.0"
edition = "2021"
description = "Wire decision continuations to heartbeat runtime"
license = "Apache-2.0"

[dependencies]
serde_json = "1"
tokio = { version = "1", features = ["rt", "macros"] }

[lib]
path = "src/lib.rs"
