[package]
name = "pc-json-schema-secret-refs"
version = "0.1.0"
edition = "2021"
description = "JSON schema secret-ref extraction + dot-path config access (pure logic)"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
uuid = { workspace = true }

[lib]
path = "src/lib.rs"
