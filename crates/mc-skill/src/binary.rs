//! 「这个文件 / 这段内容是不是二进制」的判定（导入时静默跳过）。
//!
//! - **写者**：M6-2（`docs/57` §3.2）。
//! - **上游**：`internal/skill/binary.go` 的 `IsLikelyBinaryFilePath`（按扩展名/后缀）与
//!   `IsLikelyBinaryContent`（按 NUL / 不可解码字节）。
//! - **为什么必须有**：`skill_file.content` 是 `TEXT` 列。二进制字节（PNG / 字体 / 内层
//!   归档）写进去会撞 SQLSTATE 22021；上游的选择是**跳过并留日志**，而不是让整包导入失败
//!   （`internal/handler/skill.go` 的 `addFile`）。照抄这个取舍。
//! - **本仓约定**：两个函数都返回 `bool`，**纯函数无 IO**（可单元测试）；判定表与上游逐条对齐，
//!   **不要**顺手加新扩展名（会把上游能导入的包判掉）。
//! - **不做什么**：不做 MIME 探测（不引新依赖）、不做内容转码（跳过就是跳过）。
//!
//! **状态：M6-2 待落地**（本文件由 M6-0 anchor 建为 doc-only 桩）。
//!
//! 行预算（门 ⑩）：预计 90 行以内（一张常量表 + 两个函数 + 用例）。
