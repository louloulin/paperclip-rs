//! 保留路径判定 ——「哪些路径不能当 `skill_file` 存」。
//!
//! - **写者**：M6-2（`docs/57` §3.2）。
//! - **上游**：`internal/skill/reserved.go` 的 `IsReservedContentPath`。
//! - **语义（唯一实现点）**：`SKILL.md` 本身**不是**支持文件 —— 它的正文是
//!   `skill.content`（列），不是 `skill_file` 的一行。导入与手工写文件两条路径都必须
//!   在写库前用它挡一道，否则会出现「同一份正文两处存」且刷新时互相覆盖。
//! - **本仓约定**：返回 `bool`，大小写与路径规范化口径与上游一致（比较前做与上游相同的
//!   规范化，不要自行加 `to_lowercase`）。
//! - **不做什么**：`validateFilePath`（路径合法性 / `..` 穿越）是 route 层的入参校验，
//!   归 M6-2 的 `routes/skills/files.rs`，**不**搬到这里（一个在写库前、一个在解归档时，
//!   两者都要能独立复用）。
//!
//! **状态：M6-2 待落地**（本文件由 M6-0 anchor 建为 doc-only 桩）。
//!
//! 行预算（门 ⑩）：预计 60 行以内。
