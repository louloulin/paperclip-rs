[package]
name = "pc-environment-execution-target"
version = "0.1.0"
edition = "2021"
description = "Resolve AdapterExecutionTarget from environment + adapter context"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
path = "src/lib.rs"
