[package]
name = "pc-run-continuations"
version = "0.1.0"
edition = "2021"
description = "Re-exports from run-liveness / issue-graph-liveness"
license = "Apache-2.0"

[dependencies]

[lib]
path = "src/lib.rs"
