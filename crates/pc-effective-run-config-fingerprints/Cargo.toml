[package]
name = "pc-effective-run-config-fingerprints"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Effective run config fingerprints (session/workspace/lease) with stable canonical SHA-256"

[lints]
workspace = true

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
sha2 = { workspace = true }
hex = { workspace = true }
regex = { workspace = true }
once_cell = "1"

[dev-dependencies]
serde_json = { workspace = true }
