[package]
name = "pc-url-keys"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "URL key normalization for agents and projects (port of paperclip packages/shared/src/agent-url-key.ts + project-url-key.ts)"

[lints]
workspace = true

[dependencies]
once_cell = "1"
regex = { version = "1", default-features = false, features = ["std", "unicode-case", "unicode-perl", "unicode-script"] }
