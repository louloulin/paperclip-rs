[package]
name = "pc-agent-invokability"
version = "0.1.0"
edition = "2021"
description = "Evaluate agent invokability based on status + org chain"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
path = "src/lib.rs"
