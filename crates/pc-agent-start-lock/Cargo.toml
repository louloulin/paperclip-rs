[package]
name = "pc-agent-start-lock"
version = "0.1.0"
edition = "2021"
description = "Per-agent start lock with stale-timeout bypass"
license = "Apache-2.0"

[dependencies]
tokio = { version = "1", features = ["sync", "time", "rt", "macros"] }
once_cell = "1"
serde_json = "1"

[lib]
path = "src/lib.rs"
