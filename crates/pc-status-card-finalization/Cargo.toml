[package]
name = "pc-status-card-finalization"
version = "0.1.0"
edition = "2021"
description = "Pure helpers for status-card finalization (stalled generation)"
license = "Apache-2.0"

[dependencies]

[lib]
path = "src/lib.rs"
