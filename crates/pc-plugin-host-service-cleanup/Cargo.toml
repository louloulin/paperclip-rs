[package]
name = "pc-plugin-host-service-cleanup"
version = "0.1.0"
edition = "2021"
description = "Cleanup plugin host services on worker stop/unload events"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
path = "src/lib.rs"
