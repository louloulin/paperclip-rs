[package]
name = "pc-heartbeat-stop-metadata"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Heartbeat run stop metadata: timeout policy, stop reason inference, and result JSON merge"

[lints]
workspace = true

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
