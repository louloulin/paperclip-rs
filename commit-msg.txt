feat(m2-e): label / property definition catalogs (LUM-1370)

上游 /api/labels* 与 /api/properties* 两块定义目录的真实实现，
并把 M2-A 遗留的 3 条 labels 501 桩换成真 handler（含补上 POST）。

crates/mc-repos
- label.rs：LabelRepo（list/get/create/update/delete/list_for_issue/
  attach_to_issue/detach_from_issue），resource_type ∈ {issue,agent,skill}，
  usage_count 按 resource_type 在三张关联表间切换计数（同上游 issue_label.sql）；
  同事务先清关联再删行（关联表无 FK）；颜色 ^#?[0-9a-fA-F]{6}$ 归一为小写。
- property.rs(+property/validation.rs)：PropertyRepo（list/get/create/update/
  active_count/definition_exists），create/update 走 props:<ws> advisory lock
  下的 read-then-write（活跃上限 20，并发用例实证）；定义只归档不删除，
  type 不可变；值面 definition_for_value/validate_value/resolve_actor_refs/
  bag_exceeds_limit（9 类型 + actor "<kind>:<uuid>" + 16KB 预检）。

crates/mc-http
- routes/labels.rs、routes/properties.rs（各自 router()，目录面含尾斜杠双形态，
  逐条对应上游 chi Mount 的两个形态）。
- issues/mod.rs：/labels 三键原地换真 handler；issues/extras.rs 的 set_property/
  delete_property 改成"定义面先行"（顺序逐字对齐上游 SetIssueProperty）；
  issues/dto.rs 新增三态 SetPropertyValueRequest（缺字段 vs 显式 null）+ 守卫单测。
- 门 ⑦：local 329→348（+19 键 = 10 规范形态 + 9 别名），implemented 265→275，
  known_gap 191→181，regressions 0，owners.M2-E 9→0（外加闭环 M2-A 的
  POST /api/issues/{id}/labels）。零迁移（schema 已由 migrations/upstream 覆盖）、
  零新依赖、Cargo.toml/lock 与迁移目录一行未动。

测试：mc-repos 16 纯单测 + 6 真库用例 + mc-http 2 e2e（真库 + 真 router）+
dto 守卫 1 例；两条既有测试因行为变更而改（501 canary 换到 pull-requests）。
R7 拆分：property.rs→property/validation.rs；property/tests.rs→property/db_tests.rs
（门 ⑩ 800 行上限）。

门禁：bash scripts/gates.sh --with-db → 10/10 green（78s）。
文档：新增 docs/59-M2-E-LABEL-PROPERTY.md；更正 docs/10 §5.1/§5.2、docs/11 §6/§2.4。
