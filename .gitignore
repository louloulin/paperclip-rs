node_modules
dist/
public/wasm/
.DS_Store
*.env
.env*

# Rust / Tauri
target/
src-tauri/target/
src-tauri/WixTools/
src-tauri/resources/vc_redist.x64.exe
src-tauri-mobile/google-services.json
src-tauri-mobile/gen/android/app/google-services.json

# Vendored C/C++ binaries (kreuzberg pdfium prebuilt; populated by scripts/setup-pdfium.sh)
vendor/pdfium/

# Machine-local cargo config; written by scripts/setup-pdfium.sh with
# the absolute path to vendor/pdfium. Safe to delete and re-run setup.
.cargo/config.local.toml
.dev-logs/
# Python (skill-creator scripts, eval helpers)
__pycache__/
*.pyc
*.pyo
.vite*/
# LumosAI runtime data
.lumosai/

# IDE
.vscode/
.idea/
*.swp
tool_comparison.md

# Private research
docs/research/

# Local assistant state
.claude/

# Playwright e2e test artifacts (trace, screenshot, video, html report)
/playwright-report/
/test-results/
/blob-report/
/playwright/.cache/

# Codegraph daemon runtime state
.codegraph/

# User-level agent runtime data (created at runtime, not source)
/agents/

# Playwright MCP local logs (browser session recordings)
.playwright-mcp/

# Backup files (temporary during refactoring)
*.bak

# Build / runtime artifacts (not source)
librust_out.rmeta
logs/
data/
newapi-token-modal.png

# Local secrets for dev workflow (newapi token, etc.)
# See lumosai.env.local.example for the template.
lumosai.env.local

# Browser/Playwright verification screenshots (comet-verify, comet-open phases).
# Each successful completion drops `vX.Y.Z-*.png` at repo root and they
# are NOT source code — gitignore them like newapi-token-modal.png above.
v*.*.*-*.png
# Admin debug screenshots (comet-verify, comet-hotfix)
admin-*.png
# Pending/analysis/review screenshots — non-source, non-doc
*-fixed.png
# Build log artifacts (just prod / dev) 
.just-prod.log

# Standalone pnpm build log (when invoking `pnpm build` outside `just prod`)
.pnpm-build.log

# Server bare-binary packaging output (produced by `just prod` / scripts/build-server-package.mjs)
dist-package/
.vercel

# Slides workspace output (pptxgen / Office temp locks)
slides/bp/output/

# A2A public discussion draft (working notes, not committed)
a2a-public-discussion.md

# OPC work artifacts (community application forms, branding design)
output/

# One-shot ad-hoc helpers (generate test code, then delete)
voice-lang-tests.py
voice-overlay-lang.py

# Audit script outputs (generated by scripts/audit/*.mjs — generated_at timestamps)
audit/parity-inventory.json
audit/stub-usage.csv
audit/.coverage-trend.json

# Codex IDE cache (thumbnails, image previews — generated by toolchain)
.codex-cache-*.png
.codex-cache-*.jpg
.codex-table-thumb.jpg
target/