# paperclip-rs 综合验证 - 进度快照 v5 (2026-08-16 13:30)

## 本轮 (R672) 关键成果

### R672 - pipeline-conversation-context 完整接入 + 真实 PG 验证
- crates/pc-http/src/routes/cases.rs 新增 2 endpoint + 6 unit test
- **关键 bug 修复**：handler 错误用 `CaseRepo::get` 查 issue-side `cases` 表，
  pipeline cases 实际在 `pipeline_cases` 表。新增 `lookup_pipeline_case_company_id` helper
  直接 SQL 查询 `SELECT company_id FROM pipeline_cases WHERE id=$1`
- **真实验证链路**：POST /api/pipelines（company_id 字段）→ POST stages（kind=working）→ 
  POST cases（case_key 必填）→ GET body-context / body-context.md
- 端到端响应：
  - `GET /api/cases/{case_id}/body-context` → 200 + `{bodyDocument:null, caseId, openAnnotationThreads:[]}`
  - `GET /api/cases/{case_id}/body-context.md` → 200 + 完整 markdown（含 case_id、API 提示、redaction 规则）
  - 不存在的 case → 404 + `{error:"not found: case ..."}`

## 综合覆盖度（最新）

### 模块层

| 维度 | Node | Rust | 覆盖度 |
|------|-----:|-----:|------:|
| 核心域 routes 文件 | 60 .ts | 76 .rs | **100%** |
| Route 注册 paths | 487 | 757 | **100%** |
| 核心域 services | 193 | 105 pc-* crates | **100%** |
| Code LOC | 444,337 | 548,225（含 tests） | 1.23x |

### 测试层（真实 PG / HTTP / WS）

| crate | tests | 状态 |
|-------|------:|:--:|
| pc-http lib | **495** | PASS 100%（R672 +6） |
| pc-auth lib | 80 | PASS 100% |
| pc-routines lib | 41 | PASS 100% |
| pc-realtime | 94 | PASS 100% |
| pc-heartbeat | 1054 | 1053/1 (r558 pre-existing) |
| pc-repos | 634 | PASS 100% |
| pc-agent-jwt unit | 17 | PASS 100% |
| pc-pipeline-conversation-context | 25 | PASS 100% |
| workspace 单测 | 5834 | PASS 100% |
| 综合 e2e | **68+** | PASS 100%（R672 +4 端到端） |

### 关键模块 parity

| Node 模块 | Rust crate | 状态 |
|---|---|---|
| pipeline-conversation-context | pc-pipeline-conversation-context | ✅ R672 完整接入 |
| environment-runtime | pc-environment/runtime_parity | ✅ R671 1:1 |
| cron/scheduler | pc-routines + pc-server | ✅ R669 |
| heartbeat | pc-heartbeat | ✅ R660 |
| agent-jwt | pc-agent-jwt | ✅ R661 |
| status-cards | pc-repos/status_card | ✅ R662 |
| realtime | pc-realtime | ✅ R658 |
| webhook | pc-http webhook | ✅ R657 |
| workspace | pc-workspace (5834 tests) | ✅ R669 |
| auth/boundary | pc-http middleware + auth_layer | ✅ R664 |
| issue-sub-services | pc-http/routes/issue_subservices | ✅ R666 |
| workspace-runtime | pc-http/routes/workspace_runtime | ✅ R665 |

### 累计进度评估

**核心域**：~98.5%
**UI/客户端**：待定（用户硬约束暂缓）
**Adapter 层**：13 个 0%（用户硬约束暂缓）
**远程执行**：~85%（heartbeat + scheduler + runtime 基本完成）

**综合加权完成度**：~98.5%（核心域）

## 后续路线（R673+）

### 高优先级
- **R673**：跨域 cross-field 一致性测试（如 issue ↔ decision 关联、pipeline ↔ stage 联动）
- **R674**：完整复刻 `environment-config.ts` / `environment-execution-target.ts`
- **R675**：检查并补完 Node 其他 1:1 service parity 缺口

### 中优先级
- **R676**：Node cron/scheduler parity 进一步深入
- **R677**：环境变量与 secret binding 全链路 e2e 验证

### 低优先级（用户硬约束延后）
- Adapter 13 个 100%
- UI / frontend client
- 远程 agent execution 完整对接

## 用户硬约束遵守（持续）

| 约束 | 状态 |
|---|---|
| 不 commit | ✅ |
| 不修 Adapter | ✅ |
| 真实验证 | ✅（PG + HTTP + WS + 真实启动 server） |
| 中文 evidence | ✅（R657-R672 共 16 篇） |
| 不修预存在 unrelated bug | ✅ |
| 不调 `update_goal` 完成 | ✅（目标远未完成，仍在持续推进） |
| 继续推进 | ✅ |

## 重大事件记录

- **R672 重建事件**：本次 R672 实施过程中曾因 `git checkout crates/pc-http/src/routes/cases.rs`
  误将 R672 working dir 修改还原（R672 从未 commit 或 stash），导致 6 unit test 临时丢失。
  通过精确 js 重写文件恢复 R672 全部内容（含正确 API 调用 + 修复 pipeline_cases vs cases 表 bug）。
  全程未影响其他 crate，cargo test 全量 495 passed / 0 failed。